{
  "name": "Amoji Engine",
  "install": "git fetch origin main && git reset --hard origin/main && cd amoji-engine && npm ci && npm run build",
  "terminals": [
    {
      "name": "lab",
      "command": "cd amoji-engine && npm run lab"
    }
  ],
  "ports": [5173]
}
